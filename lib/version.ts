// Bump this whenever shipping a visible change so it's easy to confirm which
// build is actually loaded in the browser (helps diagnose stale caches).
export const APP_VERSION = "build 8 · day ↔ habits linked";
